[build]
  command = "npm run build"
    publish = "dist/client"

    [build.environment]
      NODE_VERSION = "20"
        SERVER_PRESET = "netlify"

        [[redirects]]
          from = "/*"
            to = "/.netlify/functions/server"
              status = 200
